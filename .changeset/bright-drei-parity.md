---
'@octanejs/drei': patch
---

Publish the complete `@octanejs/drei` port pinned to `@react-three/drei` 10.7.7.

The package now covers the pinned web API surface with Octane-native components and hooks, executable runtime and type parity evidence, and documented divergences for React-only behavior.
