---
"@octanejs/solana-react": patch
---

Align `useRequestQuery` with upstream Kit sources (`ReactiveActionSource`, `getAbortSignal`) and expand pristine/adapted parity evidence for the query entry point.
