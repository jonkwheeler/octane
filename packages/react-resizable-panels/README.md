# @octanejs/react-resizable-panels

Work-in-progress Octane port of `react-resizable-panels@4.12.2`.

The U1 package shell intentionally does not claim runtime parity. See
[`UPSTREAM.md`](./UPSTREAM.md) for the pinned provenance and contract inventory.
