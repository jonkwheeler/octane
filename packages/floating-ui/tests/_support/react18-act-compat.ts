// @ts-ignore npm alias is runtime-only; @types/react describes the same pinned package.
export * from 'react-upstream';
// @ts-ignore npm alias is runtime-only; @types/react describes the same pinned package.
export { default } from 'react-upstream';
export { act } from 'react-dom/test-utils';
