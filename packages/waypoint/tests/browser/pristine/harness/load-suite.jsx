/**
 * Loads the byte-exact upstream Karma browser suite against the vendored React
 * binding. react-dom is redirected to the React 19 legacy shim.
 */
import '../../../../upstream/test/browser/waypoint_test.jsx';
