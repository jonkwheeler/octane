{
  "schemaVersion": 1,
  "upstreamRoot": "packages/alien-signals/audit/type-probes",
  "adaptedRoot": "packages/alien-signals/typetests",
  "inventories": {
    "upstream": "packages/alien-signals/audit/pristine-types.json",
    "adapted": "packages/alien-signals/audit/adapted-types.json"
  },
  "lanes": {
    "pristine": {
      "compiler": "tsc",
      "project": "packages/alien-signals/audit/type-probes/tsconfig.pristine.json",
      "sourcePolicy": "Repo-authored React declaration probes against the vendored react-alien-signals 0.3.0 surface, including negative controls."
    },
    "adapted": {
      "compiler": "tsrx-tsc",
      "project": "packages/alien-signals/typetests/tsconfig.json",
      "sourcePolicy": "Octane public type probes with matching assertion groups for the @octanejs/alien-signals surface."
    }
  },
  "permittedTransformations": [
    {
      "kind": "import-root",
      "rule": "React probes import vendored upstream/src; Octane probes import @octanejs/alien-signals"
    },
    {
      "kind": "negative-control",
      "rule": "Matching @ts-expect-error and expectType groups must reject or accept the same shapes on both sides"
    }
  ],
  "notes": "Upstream ships no dedicated typetest suite; both lanes are repo-authored probes over the pinned runtime surface. Assertion groups are compared one-for-one. Computed-signal acceptance for useSignalValue is an Octane-only divergence (alien-signals-readable-computed), not a paired-probe transformation.",
  "divergences": [
    {
      "id": "alien-signals-readable-computed",
      "rule": "Paired probes keep useSignalValue(count); readable computed acceptance lives in typetests/readable-computed.test-d.ts"
    }
  ]
}
