# @octanejs/number-format

Octane renderer port of `react-number-format@5.4.5`.

The formatting, caret management, and public API track upstream. Components
render through Octane and do not require React at runtime.
