import type {
  TitleCssVariables,
  TitleFactory,
  TitleOrder,
  TitleProps,
  TitleSize,
  TitleStylesNames,
} from './Title.tsrx';

export { Title } from './Title.tsrx';

export type {
  TitleProps,
  TitleOrder,
  TitleSize,
  TitleStylesNames,
  TitleCssVariables,
  TitleFactory,
};
