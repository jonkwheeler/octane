# TanStack Start React islands

This example keeps the root document, file routes, SSR, hydration, loaders, and
server functions in React. The index route mounts one Octane TSRX leaf through
`OctaneCompat`.

Do not convert route modules, the root document, or server functions.
